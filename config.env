# 小白搜盘本地测试配置（sudo appcenter-cli install-local --env config.env）

APP_PORT=12668
PROXY=
AUTH_ENABLED=false
AUTH_USERS=
AUTH_TOKEN_EXPIRY=24
AUTH_JWT_SECRET=
CACHE_TTL=60
MAX_CONCURRENCY=200
MAX_PAGES=30
data_action=keep
